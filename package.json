{
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:rules": "firebase emulators:exec --project demo-asset-tracker-rules-test --only firestore \"vitest run tests/firestoreRules.test.js --config vitest.rules.config.js\"",
    "test:all": "npm run test && npm run test:rules"
  },
  "dependencies": {
    "yahoo-finance2": "^3.14.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.30.2",
    "jsdom": "^29.1.1",
    "vitest": "^5.0.1"
  }
}
